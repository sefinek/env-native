{
  "name": "dotenv-benchmark",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Sefinek <contact@sefinek.net> (https://sefinek.net)",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "up": "ncu -u && npm install && npm update && npm audit fix"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.48.3",
    "dotenv": "^17.2.1"
  }
}
